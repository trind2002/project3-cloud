apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-feed
  labels:
    part: backend-feed
spec:
  replicas: 2
  selector:
    matchLabels:
      part: backend-feed
  template:
    metadata:
      labels:
        part: backend-feed
    spec:    
      volumes:
        - name: aws-secret
          secret:
            secretName: aws-secret
      containers:
      - name: udagram-api-feed
        image: trind7/udagram-api-feed
        resources:
          requests:
            memory: "500Mi"
            cpu: "250m"
        volumeMounts:
          - name: aws-secret
            mountPath: "/root/.aws/"
            readOnly: true
        ports:
        - containerPort: 8080
        env:
          - name: POSTGRESS_USERNAME
            valueFrom:
              secretKeyRef:
                name: env-secret
                key: POSTGRESS_USERNAME 
          - name: POSTGRESS_PASSWORD
            valueFrom:
              secretKeyRef:
                name: env-secret
                key: POSTGRESS_PASSWORD
          - name: POSTGRES_HOST
            valueFrom:
              configMapKeyRef:
                name: app-config-map
                key: POSTGRES_HOST
          - name: POSTGRES_DB
            valueFrom:
              configMapKeyRef:
                name: app-config-map
                key: POSTGRES_DB
          - name: AWS_BUCKET
            valueFrom:
              configMapKeyRef:
                name: app-config-map
                key: AWS_BUCKET
          - name: AWS_REGION
            valueFrom:
              configMapKeyRef:
                name: app-config-map
                key: AWS_REGION
          - name: AWS_PROFILE
            valueFrom:
              configMapKeyRef:
                name: app-config-map
                key: AWS_PROFILE
          - name: URL
            valueFrom:
              configMapKeyRef:
                name: app-config-map
                key: URL
          
          - name: JWT_SECRET
            valueFrom:
              secretKeyRef:
                name: app-config-map
                key: JWT_SECRET